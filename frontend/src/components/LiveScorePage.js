// LiveScorePage component removed per project requirements.
// Kept as a placeholder file to avoid accidental imports elsewhere.
import React from 'react';

const LiveScorePage = () => {
    return (
        <div className="text-center p-6 text-white">
            Live score feature has been removed from this project.
        </div>
    );
};

export default LiveScorePage;